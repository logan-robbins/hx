---
description: Check the hx installation and instance for what is missing or broken.
---

# /hx:doctor — installation health

Run `hx doctor` and explain each line that is not ok: what is missing, what is broken,
and the concrete next step to fix it. Do not fix anything without being asked — report
first. If `hx` is not installed or no instance exists, point at `/hx:setup`.
