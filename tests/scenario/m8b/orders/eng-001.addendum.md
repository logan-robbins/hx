Answering what you stopped on: **JSON only on stdout.** The pipeline is the point of the flag,
and a human-readable line in front of it makes the output unparseable by exactly the scripts
the flag exists for.

So: with `--json`, stdout carries the JSON object and nothing else — no leading greeting, no
trailing newline beyond the one `print` adds. Without `--json`, nothing changes.

Criterion 3 in the definition of done was wrong and I am withdrawing it. The scripts that grep
for `Hello,` are mine to fix, and they will keep working anyway as long as they do not pass
`--json`. Do not compromise the pipeline to keep them happy, and do not try to satisfy both by
putting the greeting on stderr — that is a third behaviour nobody asked for.

You were right to stop rather than pick. Keep the `## Tasks` you have and the commits you have
already made; you do not need to re-read `greet.py`, and your notes on it still hold. Add the
test for the JSON-only shape and finish.
