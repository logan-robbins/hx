# gtm-10: the texts against the real M8 run

Sent after build-10. Read `goals/build-10.done.md`, `goals/build-9.done.md`
(`handoff/build-to-gtm.md` has the Companion eval findings).

1. `companion/BASE.md` and `roles/*.md`: apply the eval findings; re-run the pack test.
2. `docs/getting-started.md`, `docs/operating.md`, `README.md`: every command and output
   re-captured from the M8 run; nothing invented.
3. Skills: what the run showed the Partner and workers actually needed to be told.
4. `CHANGELOG.md` for M7–M8. `tools/milestone-check.sh gtm` passes; committed path-scoped;
   `goals/gtm-10.done.md` short.
