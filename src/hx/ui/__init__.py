"""The hx UI (spec 16): a read-only localhost view of one instance.

`hx.ui.server.serve(root)` is what the `hx ui` CLI entry calls. `hx.ui.data`
holds the one interface every view reads through.
"""
